import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Inventory App! - Inventory",
};

export default function Page() {
  return (
    <div>
      <h1>Welcome to the Inventory page!</h1>
    </div>
  );
}
