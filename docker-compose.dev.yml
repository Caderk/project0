services:
  frontend:
    build:
      context: ./frontend-service
    ports:
      - "3000:3000"
    networks:
      - webnet

  inventory:
    build:
      context: ./inventory-service
    ports:
      - "3001:3001"
    networks:
      - webnet

  nginx:
    build:
      context: ./nginx
      args:
        NGINX_CONF: dev.conf
    ports:
      - "80:80"
    depends_on:
      - frontend
      - inventory
    networks:
      - webnet

networks:
  webnet:
